# useForm

Ejemplo de uso

```
    const initialForm = {
        name: 'custom_name',
        age:; 10,
        email: 'custom_mail@mail.com'
    };

    const { formValues, handleInputChange, reset } = useForm( initialForm );
```